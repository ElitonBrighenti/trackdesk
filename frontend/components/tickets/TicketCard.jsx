// TODO: Implementar TicketCard
// Exibe: badge de prioridade, número do ticket (#W-XXXX), título,
// resumo da IA, avatar do responsável, tempo
// Draggable para mover entre colunas do Kanban

export default function TicketCard({ ticket }) {
  return (
    <div>
      {/* TicketCard — aguardando implementação */}
    </div>
  )
}
